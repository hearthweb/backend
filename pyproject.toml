[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
  "bcrypt>=5.0.0",
  "fastapi[standard]>=0.136.1",
  "pydantic-settings>=2.14.1",
  "sqlmodel>=0.0.38",
]

[tool.uv]
managed = true
package = false
